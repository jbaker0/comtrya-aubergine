actions:
  - action: package.install
    list:
      - "docker"
      - "docker-compose"
      - "docker-buildx"

  - action: command.run
    privileged: true
    command: systemctl
    args:
      - "enable"
      - "--now"
      - "docker"
